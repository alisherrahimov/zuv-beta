import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ShowDetailsCard = () => {
  return (
    <View>
      <Text>ShowDetailsCard</Text>
    </View>
  );
};

export default ShowDetailsCard;

const styles = StyleSheet.create({});
